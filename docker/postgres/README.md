Postgres-specific config overrides (e.g. `postgresql.conf` tuning) go here
if/when you need to mount a custom config into the container. Empty by
default — the image's built-in defaults are fine for local development.
