# Documentation

- `HR_Portal_Dashboard_Spec.docx` — the full specification this codebase
  implements: user roles & access control, dashboard/module breakdown,
  data security & compliance, authentication & validation system,
  suggested technical approach, delivery roadmap (with sprint durations),
  and estimated costs.

Add API documentation, ADRs (architecture decision records), or runbooks
here as the project grows. The live interactive API reference is always
available at `/docs` on the running backend (Swagger UI).
